# Example workflow for plugin repositories
# Copy this to .github/workflows/changelog.yml in each plugin repo

name: Changelog Management

on:
  pull_request_target:
    types: [opened, labeled]

jobs:
  changelog:
    uses: alexcams/.github/.github/workflows/pr-prepare.yml@move-from-updatecli-to-gh-content-api
    secrets:
      token: ${{ secrets.TEST_TOKEN }}
